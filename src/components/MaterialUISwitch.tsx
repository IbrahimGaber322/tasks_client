import { styled } from "@mui/material/styles";
import Switch from "@mui/material/Switch";

// Define a styled version of the Switch component
const MaterialUISwitch = styled(Switch)(({ theme }) => ({
  // Styling for the switch itself
  width: 62,
  height: 34,
  padding: 7,
  "& .MuiSwitch-switchBase": {
    margin: 1,
    padding: 0,
    transform: "translateX(6px)",
    "&.Mui-checked": {
      color: "#fff",
      transform: "translateX(22px)",
      // Styling for the thumb (the moving part of the switch)
      "& .MuiSwitch-thumb:before": {
        // Use SVG data URL for the thumb icon
        backgroundImage: `url('data:image/svg+xml;utf8,<svg xmlns="http://www.w3.org/2000/svg" height="20" width="20" viewBox="0 0 20 20"><path fill="${encodeURIComponent(
          "#fff"
        )}" d="..." /></svg>')`,
      },
      // Styling for the track when switch is checked
      "& + .MuiSwitch-track": {
        opacity: 1,
        backgroundColor: theme.palette.mode === "dark" ? "#8796A5" : "#aab4be",
      },
    },
  },
  // Styling for the thumb element
  "& .MuiSwitch-thumb": {
    backgroundColor: theme.palette.mode === "dark" ? "#003892" : "#001e3c",
    width: 32,
    height: 32,
    "&:before": {
      // Pseudo-element for thumb's inner icon
      content: "''",
      position: "absolute",
      width: "100%",
      height: "100%",
      left: 0,
      top: 0,
      backgroundRepeat: "no-repeat",
      backgroundPosition: "center",
      // Use SVG data URL for the inner icon
      backgroundImage: `url('data:image/svg+xml;utf8,<svg xmlns="http://www.w3.org/2000/svg" height="20" width="20" viewBox="0 0 20 20"><path fill="${encodeURIComponent(
        "#fff"
      )}" d="..." /></svg>')`,
    },
  },
  // Styling for the track (the background of the switch)
  "& .MuiSwitch-track": {
    opacity: 1,
    backgroundColor: theme.palette.mode === "dark" ? "#8796A5" : "#aab4be",
    borderRadius: 20 / 2, // Round the corners of the track
  },
}));

// Export the customized MaterialUISwitch component
export default MaterialUISwitch;
